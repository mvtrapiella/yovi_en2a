server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # For any path that doesn't match a real file, serve index.html.
        # This lets React Router handle all client-side routes on page reload.
        try_files $uri $uri/ /index.html;
    }
}
