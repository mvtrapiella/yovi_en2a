ifndef::imagesdir[:imagesdir: ../images]

=== Level 1

[[section-building-block-view]]
== Building Block View

=== Whitebox Overall System

[plantuml, target=level1, format=png]
....
@startuml
skinparam shadowing false
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 60
skinparam rectangle {
    BackgroundColor #FFFFFF
    BorderColor #424242
}

title Gamey Ecosystem - Context (Level 1)

actor "Client / Player" as Client
rectangle "Gamey System" as System #F0F0F0
rectangle "Firebase" as Firebase #FFF3E0

Client -right-> System : Uses / Plays
System -right-> Firebase : Auth & Persistence
@enduml
....

Motivation::
The system uses a hybrid backend approach. **Firebase** is used as a Third-Party Building Block to offload identity management and long-term persistence. The **Gamey system** handles its own server-side session management via Redis, while delegating identity verification and data storage to Firebase.

Contained Building Blocks::
[cols="1,2" options="header"]
|===
| **Name** | **Responsibility**
| **Gamey System** | Custom and developed layers of the application with orchestrated services.
| **Firebase** | External black box providing Authentication and cloud-based data storage.
|===

==== Gamey System
Purpose/Responsibility::
Coordinates the user experience and provides a single gateway that communicates the backend and frontend interfaces.

==== Firebase
Purpose/Responsibility::
Provides a secure and external layer for handling the Data Base.

==== Game Engine
Purpose/Responsibility::
The computational core. It is agnostic of user identity (delegated to other blocks) and focuses on the game rules.


=== Level 2

==== White Box _Game Engine (Rust)_

[plantuml, target=level2, format=png]
....
@startuml
skinparam shadowing false
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 60
skinparam rectangle {
    BackgroundColor #FFFFFF
    BorderColor #424242
}
skinparam component {
    BackgroundColor #FFFFFF
    BorderColor #424242
}
skinparam database {
    BackgroundColor #F5F5F5
    BorderColor #424242
}

title Building Block View - Services (Level 2)

rectangle "Whitebox Gamey Ecosystem" #F0F0F0 {
    component [**Users**\n(API Gateway)] as Users #BDBDBD
    component [**Webapp**\n(Frontend)] as Webapp #FFFFFF
    component [**Auth Engine**] as Auth #FFFFFF
    component [**Game Manager**] as GameMgr #FFFFFF
    component [**Gamey**\n(Engine)] as Gamey #FFFFFF
    database "Redis" as Redis #F5F5F5
}
database "Firebase" as Firebase #FFF3E0

Webapp -right-> Users : All API & game requests
Users -left-> Auth : /api (Proxy)
Users -down-> GameMgr : /game (Proxy)
Users -right-> Redis : Session storage
Auth -down-> Firebase : User auth
GameMgr -right-> Gamey : Game logic
GameMgr -down-> Firebase : Match persistence
GameMgr -up-> Redis : Game state cache
@enduml
....
Motivation::
The Gamey System is decomposed into specialized services to ensure a clear separation of concerns. By utilizing a central Gateway (Users), the system provides a single entry point for the Webapp and API requests. This architecture isolates the Gamey Engine from session management, while Auth and Game Manager handle microservice-specific tasks, using Redis for high-speed state caching and Firebase for long-term persistence.

Contained Building Blocks::
[cols="1,2" options="header"]
|===
| Name | Responsibility
| Users | The central API Gateway that orchestrates and routes all incoming traffic to the appropriate internal services.
| Webapp | The frontend user interface providing the visual representation of the game and user dashboard.
| Auth Engine | Dedicated microservice that interacts with Firebase to manage user identity, tokens, and authentication flows.
| Game Manager | Orchestration service responsible for handling match state, player matchmaking, and synchronizing data with Firebase.
| Gamey | The core game engine service that executes game rules and handles CPU-intensive mechanics.
| Redis | In-memory data store used for low-latency caching of active game states and temporary session data.
| Firebase | Third-party platform providing robust authentication and cloud-hosted NoSQL data storage.
|===


=== Level 3
[plantuml, target=level3, format=png]
....
@startuml
skinparam shadowing false
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 60
skinparam rectangle {
    BackgroundColor #FFFFFF
    BorderColor #424242
}
skinparam component {
    BackgroundColor #FFFFFF
    BorderColor #424242
}

title Whitebox Detail - Components (Level 3)

rectangle "Users (Gateway)" as Gateway #BDBDBD
component [**Auth Engine**] as Auth #BDBDBD

rectangle "Whitebox Game Manager" #F0F0F0 {
    component [REST API\n(api_rest.rs)] as GM_API
    component [Data Layer\n(data.rs)] as GM_Data
    component [Firebase Client\n(firebase.rs)] as GM_Fire
    component [Redis Client\n(redis_client.rs)] as GM_Redis

    GM_API -down-> GM_Data
    GM_Data -right-> GM_Fire
    GM_Data -left-> GM_Redis
}

rectangle "Whitebox Gamey (Engine)" #F0F0F0 {
    component [Core Engine\n(core/)] as G_Core
    component [Bot Server\n(bot_server/)] as G_BotSrv
    component [Bot Logic\n(bot/)] as G_Bot
    component [Notation\n(notation/)] as G_Notation
    component [CLI\n(cli.rs)] as G_CLI

    G_BotSrv -down-> G_Core
    G_BotSrv -right-> G_Bot
    G_Core -down-> G_Notation
    G_CLI -up-> G_Core
}

Gateway -down-> GM_API : /game requests
Gateway -right-> Auth : /api requests
GM_Data -down-> G_BotSrv : Bot move request
@enduml
....
== Whitebox Game Manager (Rust)
Purpose/Responsibility::
The Game Manager acts as the bridge between the high-level game requests and the data persistence layers. It translates business logic into actionable data operations across NoSQL and cache environments.

Contained Building Blocks (Components)::
[cols="1,2" options="header"]
|===
| Name | Responsibility
| REST API (api_rest.rs) | Exposes endpoints for match management and receives commands from the Gateway.
| Data Layer (data.rs) | Contains the core business logic and decides whether to fetch data from cache or primary storage.
| Firebase Client (firebase.rs) | Handles the low-level communication and serialization for Firebase Firestore and Auth.
| Redis Client (redis_client.rs) | Manages the connection pool and CRUD operations for the volatile Redis cache.
|===

== Whitebox Gamey Engine (Rust)
Purpose/Responsibility::
This is the computational heart of the ecosystem. It is designed to be highly modular, allowing for different interfaces (CLI or Bot Server) to interact with the same deterministic game logic.

Contained Building Blocks (Components)::
[cols="1,2" options="header"]
|===
| Name | Responsibility
| Core Engine (core/) | Implements the fundamental rules, state machine, and move validation of the game.
| Bot Server (bot_server/) | A wrapper that allows the engine to be played against or by automated agents over a network.
| Bot Logic (bot/) | Contains the algorithms and decision-making heuristics for AI players.
| Notation (notation/) | Handles the parsing and generation of game-specific notation (e.g., FEN or PGN equivalents).
| CLI (cli.rs) | Provides a command-line interface for direct interaction with the engine for debugging or standalone play.
|===